<Window x:Class="WinObserver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinObserver" xmlns:viewmodel="clr-namespace:WinObserver.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:ApplicationViewModel}"
        mc:Ignorable="d"
        Title="Apparat" MinHeight="300" MinWidth="400" Height="768" Width="1024" Loaded="Window_Loaded"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="10" />
                <ColumnDefinition  Width="120" />
                <ColumnDefinition  Width="10" />
                <ColumnDefinition  Width="70" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="GetIp" Grid.Column="1" Grid.Row="1" Text="{Binding TextBoxHostname}" BorderBrush="{Binding BorderTextBox}" MouseMove="GetIp_MouseMove" MouseLeave="GetIp_MouseLeave"/>
            <Button x:Name="ControlButton" Grid.Column="3" Grid.Row="1" Content="{Binding ControlBtnName}" Command="{Binding ControlTracert}"/>
            <TextBlock Text="{Binding TextBlockGeneralError}" Grid.Column="1" Grid.Row="2" Foreground="Red" Grid.ColumnSpan="3" FontSize="14"/>
            <!--<ComboBox x:Name="GetIp" HorizontalAlignment="Left" Margin="280,0,0,0" VerticalAlignment="Center" Width="120" Text="hellos" IsEditable="True"/>-->
        </Grid>
        <TabControl DockPanel.Dock="Right" Grid.Row="1">
            <TabItem Header="{Binding NameTableDataGrid}">
                <Grid Background="Azure">
                    <DataGrid x:Name="DG1" ItemsSource="{Binding TracertObject, UpdateSourceTrigger=PropertyChanged}"  AutoGenerateColumns="False" RowBackground="GhostWhite" AlternatingRowBackground ="LightGray" GridLinesVisibility="None" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="№" Binding="{Binding NumberHostname}" Width="0.25*"/>
                            <DataGridTextColumn Header="Hostname" Binding="{Binding Hostname}" Width="*"/>
                            <DataGridTextColumn Header="Loss %" Binding="{Binding PercentLossPacket}" Width="*"/>
                            <DataGridTextColumn Header="Last Delay" Binding="{Binding LastDelay}" Width="*"/>
                            <DataGridTextColumn Header="Minimal" Binding="{Binding MinPing}" Width="*"/>
                            <DataGridTextColumn Header="Middle" Binding="{Binding MiddlePing}" Width="*"/>
                            <DataGridTextColumn Header="Maximum" Binding="{Binding MaxPing}" Width="*"/>
                            <DataGridTextColumn Header="All Packet" Binding="{Binding CounterPacket}" Width="*"/>
                            <DataGridTextColumn Header="Loss Packet" Binding="{Binding CounterLossPacket}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"/>
        <lvc:CartesianChart 
                    Grid.Row="3"
                    Series="{Binding Losses}"
                    XAxes="{Binding XAxes}"
                    YAxes="{Binding YAxes}"
                    LegendPosition="Bottom" 
                    LegendFontFamily="Console" 
                    TooltipPosition="Left"
                    >
            <!--XAxes="{Binding XAxes}"-->
            <!--TooltipPosition="Hidden" 
                    LegendPosition="Bottom"-->
            <!--ZoomMode="X" 
                    LegendFontFamily="Console"-->
        </lvc:CartesianChart>
        <Border Grid.Row="4" BorderBrush="Black" BorderThickness="0.5">
        <Grid Background="PowderBlue"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition/>
                <ColumnDefinition Width="70" />
            </Grid.ColumnDefinitions>
                <TextBlock x:Name ="AppVersion" Text="{Binding VersionProgramm}" Margin="0,4,0,0" TextAlignment="Left" Grid.Column="1" FontSize="12"  FontFamily="Console" MouseMove="TextBlockAppVersion_MouseMove" MouseLeave="TextBlockAppVersion_MouseLeave"/>
                <TextBlock TextAlignment="Center" Grid.Column="3" Margin="0,4,0,0" FontSize="12"  FontFamily="Console" MouseMove="TextBlockGithub_MouseMove" MouseLeave="TextBlockGithub_MouseLeave">
                <Hyperlink RequestNavigate="Hyperlink_RequestNavigate" NavigateUri="https://github.com/hekkaaa/Apparat">GitHub</Hyperlink>
                    <Popup x:Name="PopUpGithub"  Placement="Mouse" MaxWidth="180" AllowsTransparency="True">
                        <TextBlock x:Name="PopUpText" TextWrapping="Wrap" Width="180" Background="Cornsilk" Opacity="0.8"/>
                    </Popup>
            </TextBlock>
        </Grid>
        </Border>
    </Grid>
</Window>
