<Window x:Class="WinObserver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinObserver" xmlns:viewmodel="clr-namespace:WinObserver.ViewModel"
        mc:Ignorable="d"
        Title="Apparat" MinHeight="300" MinWidth="400" Height="568" Width="1024" Loaded="Window_Loaded" FontSize="14" FontWeight="DemiBold" FontFamily="Arial, monospace"
        WindowStartupLocation="CenterScreen">
    <!--d:DataContext="{d:DesignInstance Type=viewmodel:ApplicationViewModel}"-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <!--Top-->
        <Grid Grid.Column="0" Grid.Row="0" Background="White" Grid.ColumnSpan="3">
            
        </Grid>
        <!--Control panel-->
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!--Btn and TextBox-->
            <Grid Grid.Row="0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="GetIp" Grid.Column="0" Grid.Row="1" Text="{Binding TextBoxHostname}" BorderBrush="{Binding BorderTextBox}" BorderThickness="2" Margin="5,0,5,0" MouseMove="GetIp_MouseMove" MouseLeave="GetIp_MouseLeave"/>
                        <Button Grid.Column="1" Grid.Row="1" Content="Add" Command="{Binding AddNewHost}"/>
                    </Grid>
                    
                    <TextBlock Text="{Binding TextBlockGeneralError}" Grid.Column="1" Grid.Row="2" Foreground="Red" Grid.ColumnSpan="3" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                    <!--<ComboBox x:Name="GetIp" HorizontalAlignment="Left" Margin="280,0,0,0" VerticalAlignment="Center" Width="120" Text="hellos" IsEditable="True"/>-->
                </StackPanel>
            </Grid>
            
            <!--Left List Hostname-->
            <Grid Grid.Row="1">
                <ListBox ItemsSource="{Binding HostsCollection}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                               <StackPanel Margin="5" Orientation="Horizontal" Width="auto" Height="18" VerticalAlignment="Center">
                                     <Image Source="\Resources\icons8-error.png" Visibility="{Binding ErrorHostnameVisibleIcon}" 
                                            Width="20" Height="20" VerticalAlignment="Center"  
                                            ToolTipService.InitialShowDelay="200">
                                        <Image.ToolTip>
                                                <ToolTip BorderThickness="0" Background="LightYellow" FontSize="14" FontWeight="DemiBold" Opacity="0.9">
                                                    <TextBlock Text="{Binding TextErrorToolTip}"/>
                                                </ToolTip>
                                        </Image.ToolTip>
                                     </Image>
                                   
                                <TextBlock Text="{Binding HostnameView}" VerticalAlignment="Center" Margin="5,0,0,0" />

                                    <Button x:Name="ControlBtn" Command="{Binding StartCommand}" Width="24" Height="24" VerticalAlignment="Center" 
                                            BorderThickness="0" Background="White" Margin="5,0,5,0" IsEnabled="{Binding SettingIsEnableControlBtn}" Opacity="{ Binding SettingOpacityControlBtn}"> 
                                        <Button.Content>
                                            <Image Source="{Binding ControlBtnHost}" VerticalAlignment="Center"/>
                                        </Button.Content>
                                    </Button>
                                   
                                    <Button Command="{Binding DataContext.CloseTabCommand,RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                                            CommandParameter="{Binding}" Width="24" Height="24" VerticalAlignment="Center" BorderThickness="0" Background="White" >
                                        <Button.Content>
                                            <Image Source="\Resources\icons8-trash.png" VerticalAlignment="Center"/>
                                        </Button.Content>
                                    </Button>
                                   
                               </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="DarkSlateGray"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="BorderBrush" Value="Black"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Grid>
        
        <!--Splitte between Left panel host and DataGrid-->
        <GridSplitter Grid.Column="1" Grid.Row="1" ShowsPreview="False" Width="3"
        HorizontalAlignment="Center" BorderThickness="0"/>

        <!-- Datagrid in TabControl -->
        <Grid Grid.Row="1" Grid.Column="2">
            <TabControl DockPanel.Dock="Right" Grid.Row="1" Name="TabMain" ItemsSource="{Binding HostsCollection,UpdateSourceTrigger=PropertyChanged}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Cursor="Hand">
                            <TextBlock Text="{Binding HostnameView}" />
                            <Button DockPanel.Dock="Right" Focusable="False"
                                    Margin="10,1,0,0" Padding="0" BorderThickness="0" Background="#E8E8E8"
                                    VerticalContentAlignment="Bottom" Width="10" Height="10"
                                        Command="{Binding DataContext.CloseTabCommand,RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                                        CommandParameter="{Binding}">
                                <Button.Content>
                                    <Image Source="C:\Users\Laise\Downloads\icons8.png"/>
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <DataGrid ItemsSource="{Binding TracertObject, UpdateSourceTrigger=PropertyChanged}"  AutoGenerateColumns="False" RowBackground="GhostWhite" AlternatingRowBackground ="LightGray" GridLinesVisibility="None" IsReadOnly="True" FontSize="16">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="№" Binding="{Binding NumberHostname}" Width="0.25*"/>
                                <DataGridTextColumn Header="Hostname" Binding="{Binding Hostname}" Width="*"/>
                                <DataGridTextColumn Header="Loss %" Binding="{Binding PercentLossPacket}" Width="*"/>
                                <DataGridTextColumn Header="Last Delay" Binding="{Binding LastDelay}" Width="*"/>
                                <DataGridTextColumn Header="Minimal" Binding="{Binding MinPing}" Width="*"/>
                                <DataGridTextColumn Header="Middle" Binding="{Binding MiddlePing}" Width="*"/>
                                <DataGridTextColumn Header="Maximum" Binding="{Binding MaxPing}" Width="*"/>
                                <DataGridTextColumn Header="All Packet" Binding="{Binding CounterPacket}" Width="*"/>
                                <DataGridTextColumn Header="Loss Packet" Binding="{Binding CounterLossPacket}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>
        
        <!--Footer -->
        <Border BorderBrush="Black" BorderThickness="0.5" Grid.ColumnSpan="3" Grid.Row="3">
        <Grid Background="PowderBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition/>
                <ColumnDefinition Width="70" />
            </Grid.ColumnDefinitions>
                <TextBlock x:Name ="AppVersion" Text="{Binding VersionProgramm}" Margin="0,4,0,0" TextAlignment="Left" Grid.Column="1" FontFamily="Console" MouseMove="TextBlockAppVersion_MouseMove" MouseLeave="TextBlockAppVersion_MouseLeave"/>
                <TextBlock TextAlignment="Center" Grid.Column="3" Margin="0,4,0,0" FontFamily="Console" MouseMove="TextBlockGithub_MouseMove" MouseLeave="TextBlockGithub_MouseLeave">
                    <Hyperlink RequestNavigate="Hyperlink_RequestNavigate" NavigateUri="https://github.com/hekkaaa/Apparat">GitHub</Hyperlink>
                    <Popup x:Name="PopUpGithub"  Placement="Mouse" MaxWidth="180" AllowsTransparency="True">
                        <TextBlock x:Name="PopUpText" TextWrapping="Wrap" Width="180" Background="LightYellow" Opacity="0.8" FontSize="14" FontWeight="DemiBold"/>
                    </Popup>
                </TextBlock>
        </Grid>
        </Border>
    </Grid>
</Window>
